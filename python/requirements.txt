# ParkMálaga – dependencias Python
# Instalar con: pip install -r requirements.txt

# Descarga de datos
requests==2.32.3

# Procesado de datos
pandas==2.2.2
numpy==1.26.4

# Modelo de clasificación
scikit-learn==1.5.0
joblib==1.4.2          # serialización del modelo (.pkl)

# API bridge con Java (escritura JSON ya incluida en stdlib)
# Para Power BI: exportación via CSV/JSON (también stdlib)

# Opcional: scheduling avanzado (alternativa al while+sleep del main.py)
APScheduler==3.10.4

# Visión artificial (procesado de imagen)
opencv-python==4.10.0.84

# Deep Learning — solo necesario para train_vision.py y predict_vision.py
# No se instala en GitHub Actions (demasiado pesado para el runner de 5 min)
# Instalar manualmente en local: pip install tensorflow==2.16.1
# tensorflow==2.16.1
